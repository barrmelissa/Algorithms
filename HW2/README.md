To compile and run:

python merge4.py

There should be no output to the terminal. "ls" to see the merge4.txt file that was created. "cat merge4.txt" to view the contents.

python merge4Time.py

Array size and runtimes outputed to the screen.


                        
